---
import Layout from "../layouts/Layout.astro";
import StepCard from "../components/StepCard.astro";
---

<Layout>
  <!-- src/components/TokiFlowContent.astro -->
  <main class="flow-page">
    <!-- HERO -->
    <section class="flow-hero fade-in">
      <div class="flow-hero-inner">
        <div class="flow-hero-card">
          <div class="flow-hero-logo"></div>
        </div>
      </div>
    </section>

    <!-- INTRO -->
    <section class="flow-intro fade-in">
      <div class="flow-intro-inner">
        <h1 class="flow-title">TÓKI FLOW.</h1>

        <p>A flow where insights turn into ideas – and ideas turn into solutions that create real value for both brand, customer and planet.</p>

        <p>Our approach is based on five paths that together form the core of TÓKI Flow. They describe not just a process, but a journey – where we together move from understanding to change.</p>

        <p>Each path can stand alone, or we can move through the entire journey as one unified partnership. For us, it is not about delivering a task – but about creating movement, clarity and results that can be felt.</p>

        <div class="flow-first-step">
          <h2>First Step</h2>
          <h3>Purpose</h3>
          <p>To create a common foundation — where we understand your packaging’s journey and how it can create more value for both business, brand and planet.</p>
        </div>
      </div>
    </section>

    <!-- STEPS 1 & 2 -->
    <section class="flow-steps flow-steps--12">
      <article class="flow-step-card fade-in">
        <div class="flow-step-visual fade-in fade-delay-1"></div>
        <div class="flow-step-content fade-in fade-delay-2">
          <span class="flow-step-number">1.</span>
          <p>We start by understanding your reality. Mapping the current setup. We start with what you already know. We collect existing data and your experience — and add our perspective on what works and where potential lies.</p>
        </div>
      </article>

      <article class="flow-step-card flow-step-card--reverse">
        <div class="flow-step-content fade-in fade-delay-1">
          <span class="flow-step-number">2.</span>
          <p>Analysis of the packaging journey. We follow the packaging all the way — from factory to consumer. We see how form, materials and communication interact and where improvements can create value.</p>
        </div>
        <div class="flow-step-visual fade-in fade-delay-2"></div>
      </article>
    </section>

    <!-- STEP 3 + INFO BOXES -->
    <section class="flow-steps flow-steps--3">
      <article class="flow-step-card">
        <div class="flow-step-visual fade-in fade-delay-1"></div>
        <div class="flow-step-content fade-in fade-delay-2">
          <span class="flow-step-number">3.</span>
          <p>Common direction. We define process, milestones and decision points. This is where the common picture of how we work together emerges — with clarity and flexibility.</p>
        </div>
      </article>

      <div class="flow-extra-text fade-in">
        <div>
          <h3>TÓKI Perspective</h3>
          <p>We don't just collect insight — we connect it. Seeing the whole behind a packaging setup creates clarity that lifts both business and narrative.</p>
        </div>

        <div>
          <h3>Typical investment</h3>
          <p>This phase is usually around DKK 25–30,000 depending on scope. What matters most is starting right — with insight and direction.</p>
        </div>
      </div>
    </section>

    <!-- CTA SECTION -->
    <section class="flow-cta fade-in">
      <div class="flow-cta-inner">
        <div class="flow-cta-text">
          <h2>Want to shape the future of packaging with us?</h2>
          <p>One of five phases, where we create value – together.</p>
        </div>
        <a href="#contact" class="flow-cta-button">GET IN TOUCH</a>
      </div>
    </section>
  </main>
</Layout>
<style>
  /* TOKI FLOW CONTENT STYLES */
  :root {
    --bg-main: #f3efe7;
    --bg-card: #f9f7f2;
    --bg-cta: #8c846e;
    --bg-footer: #c8c4b3;
    --text-main: #3a3027;
    --radius-xl: 32px;
  }

  body {
    margin: 0;
    background: var(--bg-main);
    font-family: system-ui, sans-serif;
    color: var(--text-main);
  }

  /* HERO */
  .flow-page {
    padding-top: 80px;
  }

  .flow-hero {
    padding: 4rem 1.5rem 2rem;
  }

  .flow-hero-inner {
    max-width: 1100px;
    margin: auto;
  }

  .flow-hero-card {
    border-radius: var(--radius-xl);
    border: 4px solid #fff;
    background: var(--bg-card);
    padding: 5rem 0;
    display: flex;
    justify-content: center;
  }

  .flow-hero-logo {
    width: 180px;
    height: 180px;
    border-radius: 50%;
    border: 10px solid #634b34;
  }

  /* INTRO */
  .flow-intro {
    padding: 4rem 1.5rem 3rem;
  }

  .flow-intro-inner {
    max-width: 800px;
    margin: auto;
  }

  .flow-title {
    text-align: center;
    margin-bottom: 2.5rem;
    letter-spacing: 0.1em;
  }

  .flow-intro-inner p {
    line-height: 1.6;
    margin-bottom: 1rem;
  }

  .flow-first-step {
    margin-top: 3rem;
  }

  /* STEPS */
  .flow-steps {
    padding: 3rem 1.5rem;
  }

  .flow-steps--12 {
    max-width: 1100px;
    margin: auto;
    display: grid;
    gap: 4rem;
  }

  .flow-step-card {
    display: grid;
    grid-template-columns: 1.1fr 1.3fr;
    gap: 2rem;
    align-items: center;
  }

  .flow-step-card--reverse {
    grid-template-columns: 1.3fr 1.1fr;
  }

  .flow-step-visual {
    background: var(--bg-card);
    border-radius: 24px;
    min-height: 200px;
  }

  .flow-step-number {
    font-size: 4rem;
    color: #8a7d63;
    font-weight: bold;
    margin-bottom: 1rem;
    display: block;
  }

  /* Step 3 extra text */
  .flow-extra-text {
    margin-top: 3rem;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: 3rem;
  }

  /* CTA */
  .flow-cta {
    background: var(--bg-cta);
    color: #fff;
    padding: 4rem 1.5rem;
    border-radius: 32px 32px 0 0;
  }

  .flow-cta-inner {
    max-width: 1100px;
    margin: auto;
    display: flex;
    justify-content: space-between;
    flex-wrap: wrap;
  }

  .flow-cta-button {
    border: 2px solid #fff;
    border-radius: 100px;
    padding: 0.9rem 2.4rem;
    color: #fff;
    text-decoration: none;
    font-weight: bold;
  }

  /* FOOTER */
  .flow-footer {
    background: var(--bg-footer);
    color: #fff;
    padding: 3rem 1.5rem 4rem;
  }

  .flow-footer-inner {
    max-width: 1100px;
    margin: auto;
  }

  .flow-footer-tagline {
    max-width: 420px;
    margin-bottom: 2.5rem;
    line-height: 1.6;
  }

  .flow-footer-columns {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
    gap: 2rem;
  }

  .flow-footer a {
    color: white;
    text-decoration: none;
  }

  .flow-footer a:hover {
    text-decoration: underline;
  }

  .fade-in {
    opacity: 0;
    transform: translateY(20px);
    transition:
      opacity 0.8s ease-out,
      transform 0.8s ease-out;
  }

  /* Cuando entra en pantalla */
  .fade-in.visible {
    opacity: 1;
    transform: translateY(0);
  }

  /* Delays opcionales */
  .fade-delay-1 {
    transition-delay: 0s; /* izquierda */
  }

  .fade-delay-2 {
    transition-delay: 0.5s; /* derecha */
  }

  /* RESPONSIVE */
  @media (max-width: 800px) {
    .flow-step-card {
      grid-template-columns: 1fr;
    }
  }
</style>
<script>
  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add("visible");
        }
      });
    },
    { threshold: 0.1 }
  );

  document.querySelectorAll(".fade-in").forEach((element) => {
    observer.observe(element);
  });
</script>
